import {Dimensions} from 'react-native';
import Center from './Center';

const deviceWidth = Dimensions.get('screen').width;
const deviceHeight = Dimensions.get('screen').height;

export {Center, deviceWidth, deviceHeight};
